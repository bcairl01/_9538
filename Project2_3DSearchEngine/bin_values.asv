function [c,bins] = bin_values(d,min,max,res)
    N    = numel(d);
    LTM  = repmat(transpose(min:res:(max-res)),1,N);
    GTM  = repmat(transpose((min+res):res:max),1,N);
    M    = size(GTM,1);
    D    - r
    
    c    = zeros( 1, numel( bins ) );
    
    B_M  = repmat(bins.',1,size(d,2));
    D_M 

end